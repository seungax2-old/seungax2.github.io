/* import React from 'react';
import { NavLink } from 'react-router-dom';

const SubMenu = () => {
	const page = window.location.pathname
	if (page == '/main') {
		return (
			<div className="subMenu flex center">
				<NavLink to="/main/getself" className="item flex center" activeClassName="active">찾아가기</NavLink>
				<NavLink to="/main/getdelivery" className="item flex center" activeClassName="active">택배로받기</NavLink>
			</div>
		);
	}
};

export default SubMenu; */